services:
  challenge:
    build:
      dockerfile: Dockerfile.dev
    volumes:
    - ".:/app"
  trail-circuit:
    build:
      dockerfile: Dockerfile.dev
    volumes:
      - ".:/app"
  track:
    build:
      dockerfile: Dockerfile.dev
    volumes:
      - ".:/app"